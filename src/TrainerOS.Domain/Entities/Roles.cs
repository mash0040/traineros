namespace TrainerOS.Domain.Entities;

// The two role values of users.role (database.md §users).
public static class Roles
{
    public const string Trainer = "trainer";
    public const string Client = "client";
}
