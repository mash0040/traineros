# planning/ — context

This directory holds backlog notes and work-in-progress planning artifacts — the mutable layer between the fixed specs in docs/ and actual code. Nothing here is authoritative: if a note conflicts with docs/ or PRODUCT.md, the spec wins. Anything that would grow v1 scope goes through PRODUCT.md review before it becomes a task here.
